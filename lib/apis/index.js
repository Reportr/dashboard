
// Register an api
var register = function(method, uri, handler) {

};

// Init all APIs on an application
var init = function(app) {

};

module.exports = {
    init: init,
    register: register
};
